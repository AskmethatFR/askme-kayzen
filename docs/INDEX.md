# Project Documentation Graph — INDEX

> **How to use this file**
> - **Human / AI looking for an answer**: scan the table, find the node whose Title/Type matches, open its `Path`, then follow its `[[links]]` edges.
> - **Agent about to implement / decide**: this index + the relevant node(s) are the **source of truth**. Do not re-decide what a node already settles. If no node covers your question, the graph is *silent* — decide, then add/extend a node (Architect for technical, PM for functional) and a row here in the same cycle.
> - **Owners**: `architect` owns every `technical` row, `pm` owns every `functional` row. Nobody edits the other lane's nodes without routing through that owner.

## Conventions

- **ID**: kebab-case, unique, stable (rename = breaking the graph). Referenced elsewhere as `[[id]]`.
- **Type**: `technical` (Architect-owned) or `functional` (PM-owned).
- **Status**: `draft` | `current` | `superseded` | `deprecated`.
- **Updated**: ISO date `YYYY-MM-DD` of last substantive change.
- **Links**: `[[id]]` of every node this one points to (dependencies, related decisions, feature↔ADR). Bidirectional links are encouraged but only one direction is required to keep the graph connected.

## Technical nodes (owner: architect)

| ID | Title | Status | Updated | Links | Path |
|---|---|---|---|---|---|
| `adr-0001-validation-by-construction` | Validation by construction: VOs as single source of truth, events valid end-to-end; VO constructors may be fallible (`Habit::new` may not), no bypass door — and these invariants are invisible to the mutation gate. **Amended 2026-07-27**: an *in-module derivation* (`Goal::grown`/`lightened`) is the one admitted non-`new` construction path, under three conditions; every reachable `Goal` is `>= MIN` by induction; `saturating_sub` is a proof-carrying line no test can discriminate. **Amended 2026-08-17**: the three *event*-leg rows are moot (no event, no handler) and the principle is **more** visible — `AddHabit` parses three VOs and hands them straight to the infallible `Habit::new`, so reaching it with an unvalidated value is impossible, not merely discouraged; cross-habit rules were never this node's business (adr-0013) | current | 2026-08-17 | `[[architecture-overview]]`, `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0009-quality-gates]]`, `[[adr-0010-crate-boundary-trust-boundary]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/technical/adr/0001-validation-by-construction.md` |
| `adr-0002-habitboard-stateful-aggregate` | HabitBoard becomes a persisted stateful aggregate (capacity + uniqueness invariants) — **SUPERSEDED 2026-08-17 by adr-0013**. Its premise is the error corrected: the two rules are **set-based validation**, not invariants, so no aggregate could host them; `HabitBoard`, its port, its event, `BoardEntry` and `release` are deleted. Retained as history, with its three objections to "count the habits instead" judged on the delivered code — (1) *the invariant leaks out of the domain* **TRUE, a purity objection, accepted as the price**; (2) *non-atomic, two concurrent requests both pass* **UNREACHABLE**, `!Send`/`!Sync` + no suspension point; (3) *unsound, the habit only exists after the handler* **DIES WITH THE HANDLER**. Only the check precedence survives (duplicate wins on a full daily life) | superseded | 2026-08-17 | `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[architecture-overview]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/technical/adr/0002-habitboard-stateful-aggregate.md` |
| `adr-0003-two-crate-workspace` | Two-crate workspace: kayzen-core (pure domain) / kayzen-app (Dioxus shell), compiler-enforced dependency rule | current | 2026-07-16 | `[[architecture-overview]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0002-habitboard-stateful-aggregate]]` | `docs/technical/adr/0003-two-crate-workspace.md` |
| `adr-0004-routing-flat-enum` | Routing: flat Route enum mirrors designer's 6 screens (English paths, String id, views/ stubs, no layout, Android-aware navigation) | current | 2026-07-16 | `[[architecture-overview]]`, `[[adr-0003-two-crate-workspace]]`, `[[design-ecrans]]` | `docs/technical/adr/0004-routing-flat-enum.md` |
| `adr-0005-progression-suggestion-policy` | Progression modeled as read-side stability policy (suggestion, never automatic mutation; thresholds tunable, dose changes only via grow/lighten) — SUPERSEDED by adr-0008 (owner product decision → user-paced, no suggestion) | superseded | 2026-07-23 | `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[architecture-overview]]`, `[[habit-progression-study]]`, `[[adr-0002-habitboard-stateful-aggregate]]` | `docs/technical/adr/0005-progression-suggestion-policy.md` |
| `adr-0006-cqrs-light` | CQRS-light read side: query use cases + per-screen DTOs as crate-boundary contract, reads via existing ports, all derived data computed on read (stats = more queries), no projections/read store; suggestion DTO fields dropped (adr-0008); its "app never imports a domain type" MUST now carries the trust boundary (adr-0010). **Amended 2026-07-27**: the MUST scopes to *domain types* — a view naming its query's output DTO is the prescribed shape; the pre-existing `add_habit.rs` → `HabitBoardError` tension recorded as undecided. **Amended 2026-08-06**: "per-screen" is literal — a screen with two zones gets **one query, one DTO, two lists** (`TodayHabits {active, paused}`), never sibling queries, because the partitioning IS the business rule and must sit where the mutation gate can reach it; the « X sur Y » tally becomes `active.len()`, correct by construction. A domain enum crosses as a **DTO-side enum** (`HabitState`) mapped by exhaustive `match` — never the domain type, never a `bool`. **Amended 2026-08-11 (slice 6)**: the complementary half — a sibling **screen** *does* get a sibling query (`ListAnchoredHabits -> Vec<AnchoredHabit { title }>`, no `Clock`, title only), where a *zone* of one screen does not; and `anchored_count` is **derived on read** in `TodayHabits`, tallied in the same single pass that partitions active from paused — no stored counter, the classic place a projection appears. **Amended 2026-08-17**: the long-standing `HabitBoardError`-in-the-app tension is **resolved by deletion** — `app/src/services/` went with the outbox it drained, so `app/` production code imports **no domain error at all** (`.is_ok()`/`.ok()` at every call site) and the "the app never imports a domain type" MUST holds **unqualified on the write side**; neither candidate resolution (a DTO-side error contract, an error-type carve-out) was needed. General lesson recorded: an error type leaks into the app crate when the app is made to orchestrate. **Amended 2026-08-17 (PR 2)**: the MUST survives its first test — the new domain error `TransitionError` does **not** leak, because each use case maps it to a **flat variant of its own error** (`PauseHabitError::NotActive`, …) rather than wrapping it as `Refused(TransitionError)`; mechanically verified at **one file** in the workspace. The flat variant is the mechanism, not a stylistic preference **Amended 2026-08-18 (slice 7)**: the Ancrées screen's sibling query grows `id` (the readmit button's target) + `in_daily_life` (the cap's predicate, derived on read) — still one query per screen, nothing stored.| current |2026-08-18| `[[architecture-overview]]`, `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0003-two-crate-workspace]]`, `[[habit-progression-study]]`, `[[adr-0010-crate-boundary-trust-boundary]]`, `[[adr-0011-one-public-method-per-use-case]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0009-quality-gates]]` | `docs/technical/adr/0006-cqrs-light.md` |
| `adr-0007-habit-lifecycle-aggregate` | Habit promoted to lifecycle aggregate root: dated CompletionHistory + StepHistory, running dose (floor 1), LifecycleState enum, library-free LocalDate VO + infra Clock port, internal transitions (no lifecycle events published); dose facets amended to single Goal VO by adr-0008. **Amended 2026-07-27**: d2 SETTLED — `lighten()` at the floor is a silent no-op (error and UI-signal both rejected); `StepHistory {first, rest}` is append-only with no same-day fusion (d6), non-emptiness structural so `current()` is total; FUT-1 oscillation recorded as a constraint on the persistence slice. **Amended 2026-08-06 (slice 5)**: `LifecycleState` **built with `Active`+`Paused` only** (`Anchored` waits for slice 6's use case; no `Default` derive), `pause()`/`resume()` land; **AD-2** the state crosses the boundary as a DTO-side enum, never a `bool` — a second bool in slice 6 would make an impossible combo representable where it renders; **AD-3** the "aggregate methods take `today`" facet is **scoped, not blanket** — pause/resume date nothing, so neither use case holds a `Clock`; **AD-5** the board seat is pinned by a wired test, `HabitBoard` untouched. **Amended 2026-08-11 (slice 6)**: `Anchored` lands with `Habit::anchor()` — the enum is **complete** and slice 5's compile-error bet paid (the new variant broke every exhaustive `match` and handed over the worklist); **AD-4** the transitions **never refuse** — `anchor()` has no precondition and no `Result`, `MarkDone` stays byte-for-byte unchanged, and the screens decide what is offered (so the domain would let a *paused* habit be anchored, and no screen offers it); **d3 stands unamended** — anchoring publishes nothing, and the board coordination it deferred is settled in its own node. **Amended 2026-08-17 (drop-habit-board)**: `Habit` is now the **only aggregate**; **AD-5 restated** — the seat a paused habit keeps is a `state() != Anchored` **filter in `AddHabit`**, not a board entry, so the `pause-resume/S3` test now exercises the real predicate instead of the absence of one (and is load-bearing, the mutation gate generating no mutant for it — adr-0009 L5); **d3 becomes trivially true** (nothing in the codebase can publish); **AD-4 is scheduled for amendment by PR 2**, security-motivated — `Habit::resume()` has no lifecycle guard, so resuming an `Anchored` habit yields 6 non-anchored habits on a 5-seat cap (LOW, latent, unreachable by UI today, reachable at slice 7). **Amended 2026-08-17 (PR 2 `guard-lifecycle-transitions`) — AD-9**: the transition table returns to the aggregate. `pause()`/`anchor()` require `Active`, `resume()` requires `Paused`, each returning `Result<(), TransitionError>`; **9 cells, 3 legal, 6 refused**, `Paused → Anchored` refused by human ruling (*« une habitude au repos n'est pas pratiquée, donc on la reprend d'abord »*) so the path is `Paused → Active → Anchored`. AD-4 is amended, not reversed — its *product* register stands (no reproach for a permitted gesture; lightening at the floor is still a silent no-op), what changed is that **legality is the aggregate's business, not the screen's**. Guards written `if state != X`, never `match`, so the gate can generate the `!=`→`==` mutant (adr-0009 L4). Security's induction: `state` is written in exactly 4 places, only `Habit::new` increases the non-anchored count and `AddHabit` checks it — **no transition can break `\|N\| ≤ 5`**, which was the finding. `readmit()` deferred to slice 7 with two standing constraints (re-apply the **set** check itself; `resume()`'s guard must stay exactly `!= Paused`). One honest UX delta recorded: the `Active` detail renders pause and anchor side by side, so a double-click moves from *last wins* to *first wins* — cap-neutral, unverifiable without a click-dispatch harness  **Amended 2026-08-18 (slice 7)**: `readmit()` lands, `TransitionError` gains `NotAnchored`, AD-9's slice-7 cap-recheck is discharged in `ReadmitHabit` (duplicate-before-capacity read live, exactly one save, refusal leaves nothing behind) — and `resume()`'s guard stays exactly `!= Paused`, the trap held. | current | 2026-08-18 | `[[architecture-overview]]`, `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[lifecycle-backlog]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0011-one-public-method-per-use-case]]`, `[[adr-0009-quality-gates]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/technical/adr/0007-habit-lifecycle-aggregate.md` |
| `adr-0008-goal-based-dose-user-paced-progression` | Goal-based dose, user-paced progression: single Goal VO (default 5, floor 1, no ceiling, ≤5 guard dropped) replaces InitialDuration+Dose; StabilityPolicy removed (no detection/suggestion); grow/lighten always-available never suggested; StepHistory kept; owner product decision superseding adr-0005. **Amended 2026-07-27**: lighten-at-floor MAY spent (silent no-op); the floor of 1 is a **business rule** — below a minute there is no habit, and deletion is the implied (named, undesigned) exit — hence `next_goal_up`/`next_goal_down` computed in the query, never in the view | current | 2026-07-27 | `[[adr-0005-progression-suggestion-policy]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0006-cqrs-light]]`, `[[architecture-overview]]`, `[[habit-progression-study]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0009-quality-gates]]`, `[[adr-0011-one-public-method-per-use-case]]` | `docs/technical/adr/0008-goal-based-dose-user-paced-progression.md` |
| `adr-0009-quality-gates` | Quality gates: spec-only Gherkin scenario gate (scanning both crates), diff-scoped mutation testing scoped to domain + use cases + queries, one runner (`scripts/check.sh`) over shared — not vendored — gate implementations. **Amended 2026-07-26**: base-ref is mandatory and never defaulted (`HEAD~1` under-scoped every TDD slice while reporting `pass`); cargo-mutants hard-skips any `fn new`. **Amended 2026-07-27**: three further perimeter limits — L1 four view mutants held only by a `dead_code` lint that slice 5 will silence, L2 `Goal::grown`/`lightened` yield only Unviable mutants, L3 struct-literal fields are never mutated. **Amended 2026-08-06 (slice 5) — L1's premise CORRECTED and the blind spot DEMONSTRATED**: `.cargo/mutants.toml` excludes `app/src/**` by design, so the diff-scoped gate **never generated a view mutant at all** (worse than L1 claimed, not better); three hand-run mutations — resume button rewired to *pause*, whole pause-button block deleted, paused-zone guard forced to `if true` — each left the **full suite green**; mitigation pattern recorded: a gesture inline in an `onclick` is unreachable by every gate, extracted into a `#[must_use]` free function it is ordinary testable Rust. **New L4**: cargo-mutants does not mutate `match` arms, so the active/paused partition and the `LifecycleState → HabitState` mapping got **zero viable mutants** — inside the perimeter is where the instrument may look, not where it can see. Final campaign 10 generated / 7 killed / 3 unviable / 0 survived on a 1290-line diff. **Amended 2026-08-11 (slice 6)**: the perimeter is **unchanged** and was independently re-confirmed by QA and Dev-B; two additions — a **real in-perimeter survivor** (`release`'s `!=` flipped to `==`) that a *seat-count* assertion could not kill, closed by observing the **consequence that identifies the entry** (the released title), with the general lesson *a surviving mutant usually needs a different observation, not a stronger assertion on the same one*; and the **composition root's first test**, a guard that the shared `board_repository` `Rc` is not split — verified by hand-splitting it, where **108 of 109 tests stayed green** and only the guard failed. **Amended 2026-08-17 — new limit L5**: cargo-mutants mutates neither **enum variants** nor **method calls**, so `state != Anchored` → `!= Paused` and `title().matches(&title)` → `==` were **never generated**; the campaign's 11/11-killed is *not* "every guard was probed". Both guards are pinned by hand-verified tests instead (`pause-resume/S3` + `anchor-habit/S1` for the filter in both directions; `add-habit/S5` for `matches`, where **only the case difference does the work** — `HabitTitle::new` trims, neutralising the whitespace axis at construction). Dev-B found the same family a gate cannot see: an assertion whose expected value was computed from the `Display` impl under test, pinning zero words. The two slice-6 artifacts are gone (the `release` survivor with `release`; the composition-root guard test, **not** a coverage hole — its bug class is structurally impossible with one store, the residue caught transitively by the view tests), and both their lessons stand. **Amended 2026-08-17 (PR 2)**: L5's countermeasure gets a name — **three-cell triangulation**, the reusable pattern for an enum-variant equality guard (test each transition from **all three** source states, so a wrong variant fails **two** tests where the gate generates zero mutants); verified by hand in two lanes. PR 2 is the first change whose correctness *depends* on the L5 mitigation. General form: **a guard the gate cannot mutate must be surrounded, not asserted**; and the limits now constrain how production code is *written* (`if !=` over `match`, so L4 does not swallow the guard). Fail-closed guards + exhaustive `match`es mean a 4th `LifecycleState` variant **breaks compilation** — the residual risk is a test gap the compiler announces. Recorded disposition **with its expiry**: swapping the returned `TransitionError` variant is `equivalent-accepted` **only because each transition has exactly one failure mode today**, making `map_err(\|_\| …)` total — it stops holding at `ReadmitHabit`, which has two | current | 2026-08-17 | `[[architecture-overview]]`, `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[adr-0003-two-crate-workspace]]`, `[[lifecycle-backlog]]`, `[[feature-catalog]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0010-crate-boundary-trust-boundary]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/technical/adr/0009-quality-gates.md` |
| `adr-0010-crate-boundary-trust-boundary` | The crate boundary is the trust boundary: `HabitId` parsed once at the use-case/query entry point (single fallible `new`, 1..=64, no trim), infallible `From<&str>` deleted, never parsed in the view; 7 escalation triggers; Ritual route knowingly uncovered. **Amended 2026-07-27**: boundary integrity rests on what the core **accepts as input**, never on what the app can **name** — DTOs are output-only (verified mechanically), so naming one is safe; the one-question test for any future core type visible in `kayzen-app`. **Amended 2026-08-06 (slice 5, Security — prospective, zero findings this cycle)**: **trigger 2 now carries two obligations at one site.** The "paused ⇒ no grow / no lighten / no ritual" rule is enforced **by the view only** — `GrowGoal`/`LightenGoal` accept a paused habit's id and `/habit/:id/ritual` is reachable by URL. A non-issue under the single-user local threat model; the day multi-user arrives it becomes a real access-control hole, and its fix lands at the **same** line as trigger 2's ownership check — the use-case entry point where the id is parsed. Both must land in one pass. **Unamended in substance 2026-08-17** — the one door is unchanged (`HabitId::new`, 1..=64, no trim, no `From`, parsed at the core's entry point); only call-site names moved (`RequestHabit` → `AddHabit`, `HabitBoardError::InvalidHabit` → `AddHabitError::InvalidHabit`), and the single non-boundary parse (the `GuidGenerator` output) **moved with the gesture — it did not multiply**. Security verified every URL-sourced `&str` entry point still parses through `HabitId::new` as its first statement; the refactor *reduced* the app's coupling (no domain error imported in `app/` production code). The seven triggers are untouched | current | 2026-08-17 | `[[architecture-overview]]`, `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0003-two-crate-workspace]]`, `[[adr-0004-routing-flat-enum]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0009-quality-gates]]` | `docs/technical/adr/0010-crate-boundary-trust-boundary.md` |
| `adr-0011-one-public-method-per-use-case` | One public method per use case (application layer only): two symmetric gestures = two types, each with its own struct, error enum, wiring and tests; the duplication is the accepted choice, not debt. Shared struct/trait/base type/`Direction` enum/generic/load→mutate→save helper all forbidden; `AdjustGoal` never exists as a code type. Rule stops at the domain — `Habit` and `Goal` stay cohesive. Human ruling, overruled the Architect's proposal | current | 2026-07-27 | `[[architecture-overview]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/technical/adr/0011-one-public-method-per-use-case.md` |
| `adr-0012-synchronous-cross-aggregate-coordination` | Cross-aggregate coordination is a **synchronous application-service orchestration, not an event**: `AnchorHabit` moves the habit to `Anchored`, saves it, then releases its board entry and saves the board — **publishing nothing** (adr-0007 d3 preserved, no `HabitEvent` enum). **Save order habit→board is deliberate**: a crash between the two loses a seat (cap stricter than the rule) instead of bypassing the cap of 5 — Security confirmed the inverse order is the exploitable one and that a bypass is not reachable today; **do not swap the blocks**. Both steps idempotent, so replay converges — idempotence instead of a transaction. Anchoring **releases the title**, which constrains slice 7: readmission re-admits an existing habit id **without** republishing `HabitRequested`. Escalation triggers: persistence, concurrency, a third coordination — **SUPERSEDED AND VOID 2026-08-17 by adr-0013**. With `HabitBoard` deleted there is one aggregate and **no gesture touches two**, so every decision here lost its subject: the save order, the idempotence-instead-of-a-transaction argument, the partial-anchor failure mode, and the slice-7 constraint (readmission is now a lifecycle transition on one aggregate whose rejection path is set-based validation — the same two guards `AddHabit` runs). Its **MUST about `HabitBoard::release` is void and actively misleading**. Two things survive: **d3** (nothing is published — and now unconditional, the outbox being deleted), and triggers (1) persistence + (2) concurrency, **inherited and sharpened** by adr-0013's Security escalation trigger. **Corrected 2026-08-17 (PR 2)**: its slice-7 sketch offered « `Habit::readmit()` *or `resume()` reused* » — the second half is **struck**, since reusing `resume()` means widening its guard to admit `Anchored` and re-opens the closed finding verbatim; and `ReadmitHabit` is **outside** PR 2's induction, so its set check is mandatory and runs before the transition | superseded | 2026-08-17 | `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[architecture-overview]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0011-one-public-method-per-use-case]]` | `docs/technical/adr/0012-synchronous-cross-aggregate-coordination.md` |
| `adr-0013-set-based-validation-outside-aggregates` | **Set-based validation lives outside aggregates** — supersedes adr-0002 and adr-0012. A rule whose predicate depends on another aggregate's **mutable** state, or on the composition of a set that changes over time, is **not an invariant** and must never be hosted by an aggregate: it lives in the **use case performing the gesture**, reading the set through the existing repository port. Carries the human's test verbatim (*« si cette valeur change pendant le cycle de vie de l'agrégat alors ce n'est pas un invariant »*) + three checks (can one instance violate it alone? does the predicate depend on another aggregate's mutable state? does the constrained set change composition?). « At most 5 » and « no duplicate title » now live in `AddHabit`, guards in that order, **one write**; `Habit::MAX_IN_DAILY_LIFE` publishes the number without hosting the rule. **`HabitBoard` was a mistake, not a design that aged out** — it existed to host a non-invariant, which forced the event, the outbox, the double write and a duplicated `HabitTitle`; it was read by **no query**. adr-0002's purity objection is **upheld and paid**; the TOCTOU objection is **unreachable by the type system** (`Rc` is `!Send`/`!Sync`, no suspension point) with **Security's three reopening conditions verbatim** — (a) async alone suffices, (b) `Arc`/`Send`, (c) storage shared across processes/tabs, the realistic one — and the fix is to move the cap **into the write**, never to re-read. Counter-example kept usable: **transition-table legality IS a real invariant** and belongs in `Habit` — **LANDED 2026-08-17 (PR 2)**, with the symmetry stated crisply: « au plus 5 » is set-based validation and lives in `AddHabit`; « on ne reprend pas ce qui est ancré » is a single-instance invariant and lives in `Habit`. Same three checks, opposite verdicts. New MUST: a **transition** that grows the set a rule bounds must re-apply the set check **in the use case** (the aggregate cannot see the set) — the standing obligation on slice 7's `ReadmitHabit`. *(MADR status: accepted)* **Amended 2026-08-18 (slice 7)**: the standing MUST gets its first application — `ReadmitHabit` re-applies the set check (same `state() != Anchored` predicate, duplicate-before-capacity, one write); the predicate now serves three sites, still never mirrored.| current |2026-08-18| `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]`, `[[architecture-overview]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0009-quality-gates]]`, `[[adr-0011-one-public-method-per-use-case]]` | `docs/technical/adr/0013-set-based-validation-outside-aggregates.md` |
| `architecture-overview` | Habit Management — workspace split (core/app), bounded context, layers (commands + queries), **trust boundary at the crate edge**, **one-use-case/one-write creation flow** (2026-08-17: the board-driven, event-mediated flow is deleted), habit lifecycle write side (single Goal VO, user-paced progression — **goal gestures at slice 3, pause/resume at slice 5, anchoring at slice 6**; `LifecycleState` **complete**: `Active`+`Paused`+`Anchored`, and no transition ever refuses), the board **releasing** an entry to free a seat + the two aggregates coordinated synchronously with nothing published, read side returning `TodayHabits {active, paused, anchored_count}` + a DTO-side `HabitState` + the Ancrées screen's own query, routed app shell with Today/Add/Detail/**Ancrées** wired incl. the adjust zone, the conditional paused zone and ancrées link, and the **extracted-free-function gesture pattern**; composition root's shared `Rc`. **Rewritten 2026-08-17 (drop-habit-board)**: `Habit` is the **only aggregate**; creation is `AddHabit` — parse VOs → read `HabitRepository::all()` filtered on `state != Anchored` → duplicate guard → capacity guard → **one save**; `MAX_IN_DAILY_LIFE = 5` is a constant **on the domain type** while the rule is the use case's; **no event, no outbox, no dispatcher, no `app/src/services/`**; the LOCAL-2 MUST "route creation through `RequestHabit`" is **replaced**, and the `HabitBoardError`-in-the-app gap is **closed by deletion**; the read-then-write window has **zero width, enforced by the type system** (`!Send`/`!Sync`, no suspension point), with Security's three reopening conditions carried in adr-0013; one latent LOW recorded — `resume()` on an `Anchored` habit yields a 6th non-anchored habit, PR 2. **Amended 2026-08-17 (PR 2 `guard-lifecycle-transitions`)**: that LOW is **closed** — the three transitions now guard themselves (`Result<(), TransitionError>`, flat use-case variants at the boundary), so **no lifecycle transition can increase the non-anchored count** and `\|N\| ≤ 5` holds by induction over the 4 sites that write `state`; the "no transition ever refuses" description above is **superseded**, `Paused → Anchored` is refused by human ruling. Slice 7's readmission carries two written constraints (re-apply the set check in the use case; `resume()`'s guard stays exactly `!= Paused`). One UX delta recorded, not fixed: pause and anchor render side by side, so a fast double-click moves from *last wins* to *first wins* — cap-neutral, unverifiable without a click-dispatch harness  **Amended 2026-08-18 (slice 7)**: readmission built — `readmit()` completes the transition table (five guarded write sites; the two +1 sites — `Habit::new`, `readmit()` — are exactly the two that check the cap before the single write), Ancrées gains the per-row « La remettre », quiet refusals and the parallel-count footer; `resume()` untouched. | current | 2026-08-18 | `[[adr-0013-set-based-validation-outside-aggregates]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0003-two-crate-workspace]]`, `[[adr-0004-routing-flat-enum]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0009-quality-gates]]`, `[[adr-0010-crate-boundary-trust-boundary]]`, `[[adr-0011-one-public-method-per-use-case]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/technical/architecture.md` |
| `design-modele-donnees` | Kaizen design — data model (Rust structs: Habit, stage, gestures, journal) | current | 2026-07-16 | `[[design-overview]]`, `[[design-gestes-kaizen]]`, `[[architecture-overview]]` | `docs/technical/design/02-modele-donnees.md` |
| `design-setup-rust-dioxus` | Kaizen design — Rust project setup tutorial (Dioxus, targets web/desktop/mobile) | current | 2026-07-16 | `[[design-overview]]`, `[[design-modele-donnees]]` | `docs/technical/design/06-mise-en-place-rust.md` |

## Functional nodes (owner: pm)

| ID | Title | Status | Updated | Links | Path |
|---|---|---|---|---|---|
| `lifecycle-backlog` | Habit Lifecycle — goal-based model (user-paced, no suggestion), 8-slice backlog (slice 4 removed), per-slice aggregate growth, **slices R1/1/2/3/3b/5/6 done — 7 (readmit) and 8 (stats) remain**; d2 settled (silent no-op at the floor); **slice 6** settled where anchoring lands you (the detail, not Today), the Ancrées screen's scope (list + count only, dots/footer deferred), aggregate-boundary debt closed (the max-5 invariant moved to `AddHabit`), and handed slice 7 the title-retaken rejection path + a Security-flagged precondition (`resume()` needs a daily-life-full guard) — left open: the copy shown when *every* habit is paused, and whether today's own bar rises when the goal is grown today **Amended 2026-08-18 (slice 7 `readmit`)**: slice 7 DONE — S1–S4 covered, Ancrées acts (readmit + refusals + footer), AD-9's "still owed — the count" closed, precondition discharged.| current |2026-08-18| `[[feature-catalog]]`, `[[glossary]]`, `[[design-principes-kaizen]]`, `[[design-gestes-kaizen]]`, `[[design-ecrans]]`, `[[architecture-overview]]`, `[[habit-progression-study]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0011-one-public-method-per-use-case]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]`, `[[adjust-goal]]`, `[[pause-resume]]`, `[[anchor-habit]]`, `[[readmit-habit]]` | `docs/functional/lifecycle-backlog.md` |
| `feature-catalog` | Habit Management — features F-1..F-7, business rules (goal ≥ 1 flexible, max 5, no duplicate), acceptance. **F-1 (slice 6)**: habit creation is now one gesture, one write via `AddHabit` — the two-step *request* → *create-on-request* flow and `HabitRequested` are retired. **F-6 (slice 5)**: a habit can be set aside and taken back in one gesture; it keeps its seat, so the five slots count non-anchored habits — the day's tally counts only what is still in the day. **F-7 (slice 6)**: anchoring **removes** the habit's entry from the daily life, freeing the seat and the title in the same act; the detail re-renders gesture-free, and the Ancrées screen ships the list + the count only, dots/footer deferred **Amended 2026-08-18 (slice 7)**: anchoring is two-way — readmission on the Ancrées screen (refusable full/duplicate), parallel-count footer shipped, dots still deferred.| current |2026-08-18| `[[glossary]]`, `[[architecture-overview]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]`, `[[design-overview]]`, `[[pause-resume]]`, `[[anchor-habit]]` | `docs/functional/feature-catalog.md` |
| `glossary` | Ubiquitous language — Habit, **Le quotidien / Daily life**, **Ajouter une habitude / Add a habit**, Title, Duplicate, Goal, Goal floor, Grandir, Alléger, Step, Step history, Practice staircase, Practice day, Completion, Mark done, État du cycle, Mettre en pause, La reprendre, Zone « En pause », **Ancrer / Ancrée, Habitudes ancrées (Ancrées screen)**, Local date, Clock **Amended 2026-08-18 (slice 7)**: **Remettre dans le quotidien (*readmit*)** added; Ancrées screen + État du cycle rows updated.| current |2026-08-18| `[[feature-catalog]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[adr-0007-habit-lifecycle-aggregate]]` | `docs/functional/glossary.md` |
| `habit-progression-study` | Progression fork settled by evidence (historical) — suggestion model SUPERSEDED by owner decision, now user-paced | superseded | 2026-07-23 | `[[design-principes-kaizen]]`, `[[design-gestes-kaizen]]`, `[[design-ecrans]]`, `[[feature-catalog]]`, `[[adr-0005-progression-suggestion-policy]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/functional/habit-progression-study.md` |
| `design-overview` | Kaizen design — product overview from designer (habit lifecycle, scope, doc map) | current | 2026-07-16 | `[[design-principes-kaizen]]`, `[[design-ecrans]]`, `[[design-gestes-kaizen]]`, `[[design-style-graphique]]`, `[[design-modele-donnees]]`, `[[design-setup-rust-dioxus]]`, `[[feature-catalog]]` | `docs/functional/design/README.md` |
| `design-principes-kaizen` | Kaizen design — non-negotiable rules (soft 5-min goal, user-paced, no streak, no guilt) | current | 2026-07-23 | `[[design-overview]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/functional/design/01-principes.md` |
| `design-ecrans` | Kaizen design — screens, gestures and transitions (6 prototype screens). **Amended 2026-08-06 (slice 5)**: the 5-habit cap counts *non-anchored* habits — a paused one keeps its seat (the literal `active = !paused && !anchored` was never right); pausing no longer returns to Aujourd'hui, since the paused detail became a rest screen; `ph-pause` deliberately unhonored (no icon font in the app). **Amended 2026-08-11 (slice 6)**: precision, not reversal — Q1 stands; the cap's "non-anchored" outcome is achieved by **removing** the board entry, not filtering it, which frees the seat and the title together; anchoring likewise re-renders the detail in place (gesture-free) instead of returning to Aujourd'hui; the Ancrées screen ships list + count only, dots/footer deferred to slice 7 | current | 2026-08-11 | `[[design-overview]]`, `[[design-style-graphique]]`, `[[lifecycle-backlog]]`, `[[pause-resume]]`, `[[anchor-habit]]` | `docs/functional/design/03-ecrans.md` |
| `design-gestes-kaizen` | Kaizen design — the seven Kaizen gestures + the loop (gesture 2 user-paced) | current | 2026-07-23 | `[[design-overview]]`, `[[design-ecrans]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/functional/design/04-gestes-kaizen.md` |
| `design-style-graphique` | Kaizen design — colors, typography, icons, components + screen captures | current | 2026-07-16 | `[[design-overview]]` | `docs/functional/design/05-style-graphique.md` |

### Gherkin feature files (spec-only — parsed by `scenario_audit.py`, never executed)

> Each scenario is materialized by a real test carrying a `// @scenario: <feature-id>/<Sn>` anchor. A scenario tagged `@wip` is specified but not yet implemented — waived from the coverage direction of the gate only.

| ID | Title | Status | Updated | Links | Path |
|---|---|---|---|---|---|
| `add-habit` | Add a habit to my daily life — 6 scenarios, all covered. Supersedes `request-habit` and `create-habit-on-request`: creation is one gesture, one write, no board and no published fact | current | 2026-08-17 | `[[feature-catalog]]`, `[[glossary]]`, `[[adr-0001-validation-by-construction]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]` | `docs/functional/features/habit-management/add-habit.feature` |
| `today-habit-list` | List the board habits for today — 3 scenarios, all covered | current | 2026-07-25 | `[[lifecycle-backlog]]`, `[[adr-0006-cqrs-light]]` | `docs/functional/features/habit-management/today-habit-list.feature` |
| `mark-done` | Mark a habit done today — 3 scenarios, all covered | current | 2026-07-25 | `[[lifecycle-backlog]]`, `[[glossary]]`, `[[adr-0007-habit-lifecycle-aggregate]]` | `docs/functional/features/habit-management/mark-done.feature` |
| `adjust-goal` | Adjust a habit's goal at the user's own pace — slice 3 delivered, **7 scenarios, all covered**. S3 and S5 pin the two boundaries symmetrically: at the floor as at the ceiling, the gesture changes nothing and records nothing. S6–S7 pin the button labels. S1/S2 reworded to drop the code jargon that had leaked into them. | current | 2026-07-27 | `[[lifecycle-backlog]]`, `[[adr-0008-goal-based-dose-user-paced-progression]]`, `[[design-gestes-kaizen]]` | `docs/functional/features/habit-management/adjust-goal.feature` |
| `practice-staircase` | Read a habit's recent practice as a staircase — slice 3b delivered, **6 scenarios, all covered**. One bar per calendar day over a fixed 7-day window, filled when practised and faint when not; the decisions staircase left the screen. S3 pins the owner's correction (adjusting draws no bar), S6 the brand-new habit (seven faint bars, standing at the goal it started on). | current | 2026-08-06 | `[[lifecycle-backlog]]`, `[[design-ecrans]]`, `[[design-principes-kaizen]]`, `[[adr-0006-cqrs-light]]`, `[[adjust-goal]]`, `[[glossary]]` | `docs/functional/features/habit-management/practice-staircase.feature` |
| `pause-resume` | Pause a habit and resume it — slice 5 delivered, **4 scenarios, all covered**. A paused habit leaves the day's list for the « En pause · aucune pression » zone and comes back in one gesture, its lived days untouched. S3 pins Q1 (it keeps its seat in the daily life, so resuming can never fail), S4 the owner's rest screen — the paused detail offers its return and its staircase, and nothing else. | current | 2026-08-17 | `[[lifecycle-backlog]]`, `[[feature-catalog]]`, `[[glossary]]`, `[[design-ecrans]]`, `[[design-principes-kaizen]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0006-cqrs-light]]`, `[[adr-0011-one-public-method-per-use-case]]` | `docs/functional/features/habit-management/pause-resume.feature` |
| `anchor-habit` | Anchor a habit that has become natural — slice 6 delivered, **4 scenarios, all covered**. S1/S2 pin the freed seat and the Ancrées listing, S3 pins Q3 (an anchored habit can still be marked done), S4 pins "no suggestion". Detail re-renders gesture-free; Ancrées ships list + count only | current | 2026-08-11 | `[[lifecycle-backlog]]`, `[[feature-catalog]]`, `[[glossary]]`, `[[design-ecrans]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0007-habit-lifecycle-aggregate]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/functional/features/habit-management/anchor-habit.feature` |
| `readmit-habit` | Put an anchored habit back into the daily life — slice 7, 3 scenarios `@wip`. S3 (a title retaken while anchored is refused as duplicate) is now load-bearing on the technical side — `[[adr-0012-synchronous-cross-aggregate-coordination]]` cites it as the constraint slice 7 must satisfy **slice 7 delivered** — 4 scenarios, all covered: S1 place again + histories untouched, S2 daily-life-full refusal, S3 retaken-title refusal, S4 the parallel-count footer (paused habit in the fixture).|current|2026-08-18| `[[lifecycle-backlog]]`, `[[anchor-habit]]`, `[[adr-0002-habitboard-stateful-aggregate]]`, `[[adr-0012-synchronous-cross-aggregate-coordination]]` | `docs/functional/features/habit-management/readmit-habit.feature` |
| `habit-stats` | Read a habit's story — slice 8, 4 scenarios `@wip` | draft | 2026-07-25 | `[[lifecycle-backlog]]`, `[[adr-0006-cqrs-light]]`, `[[design-principes-kaizen]]` | `docs/functional/features/habit-management/habit-stats.feature` |

## Graph health (maintained by the owners at end of each cycle)

- [x] Every node file has a row here; every row points to an existing file.
- [x] No dangling `[[id]]` edge (every referenced ID exists as a row).
- [x] No orphan node (every node is reachable from `architecture-overview` or `feature-catalog`).
- [x] `Updated` reflects the last cycle that touched the node.
